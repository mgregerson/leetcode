function zeroMatrix(matrix) {
  // Loop through the matrix;
  // Loop through the array
  // If there is a zero, add that [0, 2] to an list of matrices;
  // Loop through the matrix again
  // If that element has a y OR x equal to anything, it's a 0
}

/**
 * [2, 3, 1]
 * [3, 0, 6]
 * [2, 1, 3]
 *
 * [2, 0, 1]
 * [0, 0, 0]
 * [2, 0, 3]
 *
 *
 *  */
